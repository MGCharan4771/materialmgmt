_schema-version: "3.2"
ID: materialmgmt
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: materialmgmt-destination-content
  type: com.sap.application.content
  requires:
  - name: materialmgmt-destination-service
    parameters:
      content-target: true
  - name: materialmgmt-repo-host
    parameters:
      service-key:
        name: materialmgmt-repo-host-key
  - name: materialmgmt-uaa
    parameters:
      service-key:
        name: materialmgmt-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: materialmgmt_html_repo_host
          ServiceInstanceName: materialmgmt-html5-service
          ServiceKeyName: materialmgmt-repo-host-key
          sap.cloud.service: materialmgmt
        - Authentication: OAuth2UserTokenExchange
          Name: materialmgmt_uaa
          ServiceInstanceName: materialmgmt-xsuaa-service
          ServiceKeyName: materialmgmt-uaa-key
          sap.cloud.service: materialmgmt
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: materialmgmt-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: materialmgmt-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - materialmgmt.zip
      name: materialmgmt
      target-path: resources/
- name: materialmgmt
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: materialmgmt-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: materialmgmt-destination-service
    service-plan: lite
- name: materialmgmt-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: materialmgmt-xsuaa-service
    service-plan: application
- name: materialmgmt-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: materialmgmt-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
